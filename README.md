## About this project
This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).
This project was build for Javascript Web Applications (CST-391)

## Rebuild and Run!
To see this project in its development environment, rebuild and run the development server using the following commands:

```bash

npm install
npm run dev

```
